# W55RP20 감시 게이트웨이 — 구조 및 변경 인수인계

이 문서는 **이 코드베이스를 처음 보는 개발자**를 대상으로 한다.
특정 함수·변수 이름을 몰라도 읽을 수 있도록 책임과 인터페이스 중심으로 기술한다.
파일·함수 단위의 상세는 [APP_DEV_GUIDE.md](APP_DEV_GUIDE.md) 를 참조한다.

---

## 1. 시스템 개요

RP2040 + W5500 기반 감시 게이트웨이. 직렬로 붙은 장비에서 값을 수집해
HTTPS 웹 UI와 SNMP 에이전트로 노출한다. FreeRTOS SMP(2코어) 위에서 동작한다.

```
직렬 장비 ──[RS-232/422/485 × 2포트]──► 펌웨어 ──[Ethernet]──► 웹 UI / SNMP 매니저
```

내부는 4계층이다.

| 계층 | 책임 |
|---|---|
| L1 드라이버 | UART 초기화, 바이트 송수신, RS-485 방향 제어, 수신 링 버퍼 |
| L2 프로토콜 | 바이트열을 응용 프로토콜로 해석, 결과를 데이터 모델에 기록 |
| L3 데이터 모델 | 수집된 값 보관 |
| L4 표현 | 데이터 모델과 설정을 웹·SNMP로 노출 |

이번 작업의 대상은 **L1, L2, L4** 다. L3 는 설계가 이미 적절해 변경하지 않았다.

---

## 2. L1 — 드라이버 계층

### 2.1 착수 시점의 문제

| 문제 | 결과 |
|---|---|
| 단일 포트 전제로 작성됨 | 2번 포트 송신 경로가 아예 없음 |
| 포트 초기화 절차가 3벌 중복 | 세 벌이 미묘하게 달라 포트마다 동작이 갈림 |
| RS-485 방향 제어 GPIO 가 코드에 상수로 박힘 | 배선이 다른 보드에 이식 불가 |
| 수신 링 버퍼가 전역 1개 | 2포트 동시 사용 시 데이터 혼입 |

### 2.2 설계

**포트 서술자 구조체**를 도입했다. 포트에 관한 모든 정보가 이 구조체 하나에 모인다.

| 분류 | 내용 | 결정 시점 |
|---|---|---|
| 보드 배선 | UART 인스턴스, IRQ 번호, 채널 번호, TX/RX/CTS/RTS 핀, 보드 기본 DE 핀 | 컴파일 타임 상수 테이블 |
| 런타임 설정 | 실제 DE 핀, 전기 규격(232/422/485/485역상), 적용 프로토콜, 설정 블록 포인터 | 부팅 시 저장된 설정에서 채움 |

**공개 API 는 전부 이 구조체 포인터를 받는다.**

```
초기화        포트 하나를 설정대로 올린다
전체 초기화   모든 포트를 올린다 (첫 송신 이전에 반드시 실행)
1바이트 수신  대기 중인 바이트가 없으면 실패 코드
1바이트 송신
버퍼 송신     프레임 전체 동안 DE 유지, 바이트를 워드 길이로 마스킹
방향 제어 ×2  RS-485 모드가 아니면 무동작. 해제 시 시프트 레지스터 비움을 대기

수신 비우기   지난 교신의 잔여 바이트를 버린다
흐름제어 끄기 저장된 설정과 무관하게 이 포트의 RTS/CTS 를 끈다
커맨드모드 질의 지금 이 포트가 설정 처리기에 넘어가 있는지
```

**뒤의 세 개는 프로토콜이 원시 UART 핸들을 만지지 않게 하려고 있다.** 프로토콜
구현체는 `port->uart` 를 볼 일이 없고, "몇 번 포트인가"도 묻지 않는다.

### 2.3 결과

- **"어느 포트인가"를 판단하는 분기가 초기화 함수 1곳에만 남았다.** 나머지 코드는 포인터만 따라간다.
- 링 버퍼를 포트당 하나로 분리했다. API 는 상류 오픈소스(WIZnet S2E 2Port 브랜치)와
  동일한 시그니처를 유지해 채널 번호를 마지막 인자로 받는다 — 상류 변경을 나중에
  병합할 수 있게 하기 위함이다.
- 포트를 3개 이상으로 늘릴 때 필요한 작업은 **배선 테이블에 행 추가**뿐이다.

### 2.4 커맨드 모드 이스케이프 복구

네트워크가 끊겼을 때 직렬로 진입해 설정을 고치는 경로가 과거 커밋에서 통째로
삭제되어 있었다. 복구했다.

**수신 경로가 하나로 통일된 덕분에 이스케이프 감시가 프로토콜 핸들러보다 아래에
위치한다.** 즉 어떤 프로토콜이 돌든, 앞으로 추가될 프로토콜에서도 동작한다.
이스케이프의 일부로 소비된 바이트는 수신 실패 코드로 반환되어 프로토콜 데이터로
오인되지 않는다.

> 감시 대상은 1번 포트로 한정된다. 트리거 상태(인덱스, 임시 버퍼, 갭 타이머)가
> 장치 전역 단일 인스턴스이고, 실패 시 복원 경로도 1번 포트 버퍼로 밀어 넣도록
> 되어 있기 때문이다.

---

## 3. L2 — 프로토콜 계층

### 3.1 착수 시점의 문제

"이 프로토콜이 존재한다"는 사실이 **7곳에 분산**되어 있었다.

```
1. 프로토콜 열거형
2. 빌드 스크립트 소스 목록
3. 부팅 시 태스크 생성 루프
4. 기본 핸들러의 소유권 양보 판정
5. 웹 POST 검증 상한
6. 웹 UI 선택 메뉴 (HTML)
7. HTML 을 C 배열로 변환한 생성 헤더
```

**4번 누락이 가장 위험했다.** 기본 핸들러가 비키지 않으면 두 핸들러가 같은 FIFO 를
경쟁하는데, 컴파일도 통과하고 에러 로그도 남지 않는다.

### 3.2 설계

**레지스트리 테이블 1개**로 수렴시켰다. 행 구성:

| 필드 | 용도 |
|---|---|
| 식별자 | 설정에 저장되는 값 |
| 표시명 | 웹 드롭다운과 부팅 로그 |
| 태스크 진입점 | NULL 이면 전용 핸들러 없음 → 기본 핸들러가 포트를 유지 |
| 스택 크기 | 워드 단위 |
| 우선순위 | 31 이하 |

소비자가 전부 이 테이블을 읽는다.

```
              ┌──► 부팅 시 태스크 생성
              │
[레지스트리] ──┼──► 기본 핸들러의 양보 판정
              │
              ├──► 웹 POST 검증 상한 (테이블의 최대 식별자)
              │
              └──► 웹 UI 가 JSON 으로 받아 드롭다운 생성
```

**HTML 은 프로토콜 목록을 더 이상 갖지 않는다.** 장치가 설정 응답에 목록을 실어
보내고, 페이지가 그것으로 메뉴를 만든다. 따라서 6·7번 단계가 제거되었다.

### 3.3 결과

새 프로토콜 추가 절차가 **7단계 → 3단계**로 줄었다.

1. 구현 파일 작성
2. 빌드 스크립트에 등록
3. 레지스트리에 행 1개 추가

### 3.4 템플릿

**빌드되는 골격 파일**을 제공한다. 아래가 이미 구현되어 있다.

- 포트 서술자 수신 및 설정 적용
- 드라이버 API 를 통한 송수신
- RS-485 방향 제어
- 커맨드 모드 진입 시 스탠드다운
- 데이터 모델에 값 등록 (포트별 시작 행 분리)

비어 있는 곳은 **2개**다: 요청 프레임 생성, 응답 파싱.

이미 만들어진 예도 하나 있다 — **S/T/R** 이다. 직렬 한 줄을 파싱해 데이터 모델에 넣고 답을 돌려주는, 프로토콜이 할 일을 전부 갖춘 최소 예제다. 새로 만들 때 이것과 같은 모양으로 하면 된다.

> 둘 다 웹 Mode 드롭다운에는 나오지 않는다. 드롭다운은 레지스트리 표 그대로이고, 그 표는 **장비를 쓰는 사람이 고를 수 있는 것**만 담는다.
> 만든 프로토콜은 표에 자기 이름으로 추가한다.

---

## 4. L3 — 데이터 모델 (변경 없음)

**장치 N행 × 값 컬럼 M열**의 2차원 모델이다. 현재 64행 × 3열.

이 모델이 SNMP 개념 테이블과 1:1 대응하고, 웹 JSON 과 직렬 조회 명령의 응답 형태도
여기서 파생된다.

**노출 값 종류를 바꾸려면 2곳만 고치면 된다** — 컬럼 수 상수와 컬럼 서술자 배열.
SNMP 테이블 생성·갱신, 트랩 경로, 웹 JSON 이 전부 자동으로 따라온다.

제약:

- 행 수 ≤ 127 (셀 OID 서브식별자를 1바이트로 유지)
- 2 + 컬럼 수 ≤ 127 (동일 사유)
- 컬럼 1개 증가 시 SNMP 정적 배열이 64엔트리 증가 ≈ RAM 5.6 KB

---

## 5. L4 — 표현 계층

### 5.1 SNMP (변경 없음)

OID 를 루프로 생성하며 데이터 모델의 형상을 그대로 따른다. 구조적 문제가 없어
손대지 않았다.

### 5.2 웹 설정 — 착수 시점의 문제

설정 항목 하나가 **직렬화(GET)와 역직렬화(POST)에 각각 독립적으로 기술**되어 있었다.
둘을 묶는 것이 아무것도 없었다.

한쪽만 추가하면 저장은 되나 표시되지 않거나, 표시되나 저장되지 않는다.
**두 경우 모두 컴파일은 통과한다.**

### 5.3 설계

**설정 항목 테이블 1개**로 수렴시켰다. 현재 18개 숫자 항목이 등록되어 있다.

| 필드 | 용도 |
|---|---|
| JSON 키 | HTML 입력 요소의 id 와 일치해야 폼이 채워짐 |
| 필드 폭 | `sizeof` 로 자동 산출 — 손으로 적지 않음 |
| 필드 포인터 | 설정 구조체 멤버 |
| 허용 범위 | POST 검증. 벗어나면 저장하지 않음 |
| 표시 기본값 | GET 이 범위 밖 저장값을 만났을 때 대신 내보낼 값 |
| 미설정 플래그 | 저장값 0 이 "보드 기본값으로 되돌림"을 뜻하는 항목 표시 |

GET 과 POST 가 같은 테이블을 순회한다. **검증 범위와 표시 기본값이 같은 행에서
나오므로 비대칭이 발생할 수 없다.**

숫자 항목 추가 절차: **2곳 → 테이블 행 1개.**

문자열·IPv4·의미 반전 항목(DHCP 사용, 트랩 차단)은 테이블 밖에 남아 수기 처리한다.

### 5.4 해결한 함정 2개

#### 정렬 — HardFault

설정 구조체가 `packed` 다. 부트로더와 플래시 레이아웃을 공유해야 하므로 바꿀 수 없다.
결과적으로 16비트 멤버 일부가 홀수 오프셋에 앉는다.

이런 멤버를 16비트 포인터로 접근하면 비정렬 `LDRH`/`STRH` 가 생성되고,
**Cortex-M0+ 에서 HardFault 한다.** 테이블은 필드 주소를 타입 없는 포인터로 보관하므로
정확히 이 함정에 해당한다.

→ 테이블 접근자를 **바이트 단위 리틀엔디안**으로 구현했다. 바이트 접근은 항상
정렬되어 있다.

#### 버퍼 — 스택 파손

응답 JSON 을 만들 때 `n += snprintf(buf + n, sizeof(buf) - n, ...)` 관용구를 쓰고 있었다.

`snprintf` 는 **잘린 부분까지 포함해 "썼어야 할 길이"** 를 반환한다. 따라서 한 번
넘치는 순간 누적값이 버퍼 크기를 초과하고, 다음 호출의 크기 인자가 부호 없는 정수
언더플로로 **약 42억**이 된다. 그 시점부터 사실상 무제한으로 쓴다.

즉 **1바이트 초과가 절단이 아니라 스택 파손으로 이어지는 구조**였다. 절단 검사는
체인이 끝난 뒤에 있어 이미 밟은 다음에야 감지했다.

→ **경계를 강제하는 이어붙이기 함수 하나**로 일원화하고 호출 11곳을 전부 통과시켰다.
누적 길이는 그대로 유지되어 절단 검사는 계속 동작하지만, 버퍼 밖 위치나 크기가
전달되지 않는다.

> 이 지점이 중요한 이유: **가이드가 확장 시 행을 추가하라고 지시하는 자리**가 정확히
> 이 버퍼를 채운다. 현재 사용량은 최악 약 970 B / 1,536 B 이며, 남은 565 B 를 설정
> 항목과 프로토콜 목록이 공유한다.

---

## 6. 관통하는 설계 원리

세 계층의 변경이 **동일한 패턴**이다.

> 분산되어 있던 "존재 선언"을 **단일 테이블로 수렴**시키고, 소비자들이 그 테이블을
> 읽게 한다.

| 계층 | 수렴된 테이블 | 소비자 |
|---|---|---|
| L1 | 포트 배선 테이블 | 초기화, 송수신, 방향 제어 |
| L2 | 프로토콜 레지스트리 | 태스크 생성, 소유권 판정, 웹 검증, 웹 UI |
| L4 | 설정 항목 테이블 | JSON 직렬화, POST 역직렬화 |

**대상이 되는 버그 클래스가 명확하다.** 분산 기술은 한 곳을 빠뜨려도 컴파일이
통과하고 런타임에만 조용히 깨진다. 테이블 수렴은 빠뜨릴 자리 자체를 없앤다.

---

## 7. 확장 시나리오별 작업량

| 하려는 일 | 작업량 |
|---|---|
| 새 응용 프로토콜 추가 | 템플릿 복사 + 빌드 등록 + 레지스트리 1행 |
| 웹 설정 항목(숫자) 추가 | HTML 입력 + 헤더 재생성 + 테이블 1행 |
| 노출 값 종류 변경 | 컬럼 수 상수 + 컬럼 서술자 배열 |
| 직렬 포트 증설 | 배선 테이블 1행 |
| 다른 배선 보드로 이식 | DE 핀은 웹 설정, 나머지는 배선 테이블 |
| 플래시 저장 설정 추가 | 확장 영역 규칙 2개 준수 (아래) |

**플래시 저장 설정 추가 시 규칙 2가지** — 위반하면 기존 장비가 공장 초기화된다.

1. 설정 구조체 전체 크기를 바꾸지 말 것. 새 필드 N바이트를 넣었으면 예비 영역을
   정확히 N만큼 줄인다.
2. 새 필드는 **값이 0일 때 기존 동작**이어야 한다. 기존 장비의 예비 영역은 전부 0이므로
   새 필드의 초기값이 0이 된다.

---

## 8. 이번에 수정한 런타임 결함 2건

| 결함 | 증상 | 원인 |
|---|---|---|
| 커맨드 모드 태스크 동기화 객체 미생성 | 커맨드 모드 동작 불가. 부팅 직후 NULL 역참조 | 태스크는 복구했으나 그것이 대기하는 세마포어를 생성하지 않음. Release 빌드라 `configASSERT` 가 `assert()` 로 무효화되어 진입 시 검출되지 않고 그대로 역참조 |
| 설정 응답 버퍼 오버런 | 응답이 길어지면 절단이 아니라 스택 파손 | 5.4 참조 |

---

## 9. 문서

리포지토리에 [APP_DEV_GUIDE.md](APP_DEV_GUIDE.md) 가 있다(6장, 약 1,700줄).
계층별 API, 확장 레시피, 함정 모음, 작업 규칙을 담는다.

작업 중 4장에서 271줄이 실수로 삭제된 적이 있다 — "웹 설정 항목 추가"와
"SNMP OID/트랩 추가" 두 절 전체, 그리고 "새 시리얼 프로토콜 추가"의 제목.
문서 앞부분의 역할별 안내표가 이 세 절을 가리키고 있었고, 그중에는 인수인계의 핵심
동선인 "새 센서(다른 프로토콜)를 붙여야 한다" 항목이 포함되어 있었다.

복구했다. 문서 내 링크 228개를 전수 검증했고 죽은 링크는 없다.

---

## 10. 미흡한 부분

인수받는 개발자가 먼저 알아야 할 결함·공백. 계층별로 정리한다.

### 10.1 L1 드라이버

| # | 항목 | 영향 |
|---|---|---|
| 1 | **런타임 재설정 경로 없음** | 웹에서 보레이트·패리티를 바꿔도 재부팅해야 적용된다. UI 에 그 사실이 표시되지 않아 사용자는 즉시 적용된 것으로 오해한다 |
| 2 | **커맨드 모드가 1번 포트 전용** | 트리거 상태가 장치 전역 단일 인스턴스라 2번 포트로는 진입할 수 없다. 2번 포트만 연결된 현장에서는 네트워크 복구 수단이 없다 |
| 3 | **소프트웨어 흐름 제어 미연결** | XON/XOFF 구현체는 있으나 호출 경로가 죽은 모듈 안에만 있다. 설정에서 선택은 되지만 동작하지 않는다 |
| 4 | **9비트 데이터 미지원** | 하드웨어가 5~8비트만 지원한다. 설정 항목에는 남아 있고 내부에서 8비트로 클램프된다. 선택 시 경고가 없다 |
| 5 | **stick parity 검증 안 됨** | SDK 가 지원하지 않아 레지스터를 직접 조작한다. 실장비 검증 이력이 없다 |
| 6 | **DE 타이밍 마진 미측정** | RS-485 방향 전환이 순수 GPIO 토글이다. 고속 보레이트에서 마지막 바이트가 잘리지 않는지 실측이 필요하다 |
| 7 | 포트 수 상수 불일치 | 구 상수가 1로 남아 있다(실제 2). 현재 참조처는 죽은 경로뿐이라 무해하지만 오해를 부른다 |

### 10.2 L2 프로토콜

> **먼저 읽을 것 — 새 프로토콜을 추가하기 전에 아래 0번을 반드시 처리해야 한다.**
> 처리하지 않으면 코드를 아무리 정확히 작성해도 태스크가 뜨지 않는다.

| # | 항목 | 영향 |
|---|---|---|
| **0** | **프로토콜 번호 상한이 세 곳에 따로 박혀 있다** | 레지스트리는 표에서 계산하여 현재 **1**, 드라이버는 `sec_ups` 상수로 **3**, 설정툴은 `modbus_ascii` 상수로 **2** 를 쓴다. 지금은 웹이 가장 좁아서 문제가 드러나지 않는다. **4번 이상으로 행을 추가하는 순간 어긋난다** — 드라이버가 부팅 시 자기 상한을 넘는 번호를 "없음"으로 덮어쓰므로, 구현을 다 해도 태스크가 뜨지 않는다. 두 상수를 레지스트리 기준으로 바꾸는 것이 첫 작업이다 |
| 1 | **구현된 프로토콜은 2개** | S/T/R 과 Modbus RTU. 레지스트리 표에도 이 둘만 있고, 드롭다운도 그대로다. `modbus_ascii`·`sec_ups`·`protocol_custom` 은 enum 에만 있다 — 구현한 뒤 표에 행을 추가하면 드롭다운과 검증이 같이 따라온다 |
| 2 | **태스크 사망 감지 없음** | 프로토콜 태스크가 죽으면 해당 포트가 조용히 멈춘다. 감시·재시작 경로가 없다 |
| 3 | **오류 정책 지침 없음** | 템플릿에 타임아웃·재시도·연속 실패 처리에 대한 기준이 없다. 구현자마다 달라진다 |

### 10.3 L3 데이터 모델

| # | 항목 | 영향 |
|---|---|---|
| 1 | **값 신선도 판정 없음** | 갱신 시각 필드가 정의만 되어 있고 기록도 조회도 되지 않는다. **센서가 응답을 멈춰도 웹·SNMP 는 마지막 값을 유효한 값으로 계속 제공한다.** 감시 장비로서 가장 큰 공백이다 |
| 2 | **값 타입이 32비트 정수 단일** | 문자열 상태값(예: "배터리 방전 중")을 표현할 수 없다. UPS 계열에서 제약이 된다 |
| 3 | **컬럼이 전 장치 공통** | 성격이 다른 장비를 함께 붙이면 빈 칸이 생긴다. 현재는 UPS 1대만 붙이는 전제라 문제가 아니다 |
| 4 | 확장 비용 | 컬럼 1개 추가 = RAM 약 5.6 KB. 힙이 96 KB 이므로 여력이 크지 않다 |

### 10.4 L4 표현

| # | 항목 | 영향 |
|---|---|---|
| 1 | **설정 응답 버퍼 여유 565 B** | 프로토콜 목록과 설정 항목이 공유한다. 초과하면 절단되고 로그만 남는다 — 관리 수단이 로그뿐이다 |
| 2 | **POST 파싱이 문자열 검색 기반** | 커뮤니티 문자열 등에 키 형태 문자열을 넣으면 오인식이 가능하다. 인증된 사용자만 도달하므로 위험도는 낮다 |
| 3 | **웹 페이지 생성 헤더 의존성 누락** | HTML → C 배열 변환이 일부 빌드 타깃에만 걸려 있다. 해당 타깃만 빌드하면 HTML 변경이 반영되지 않는다 |
| 4 | **원 요구사항 3종 미착수** | 장비별 웹 커스터마이징 / MIB OID 편집 / 트랩 내용 편집 |
| 5 | 디버그 포트 미착수 | 직렬 원본 패킷을 웹에서 확인하는 기능 |

### 10.5 프로젝트 전반

| # | 항목 | 영향 |
|---|---|---|
| 1 | **자동 테스트 전무** | 단위·통합 테스트가 없다. 모든 검증이 실장비 수동 확인이다 |
| 2 | **죽은 코드가 앱 펌웨어의 약 13%** | 아래 표 참조. 어디가 살아있는지 코드만 봐서는 알 수 없다. 이번에 발생한 런타임 결함 1건이 정확히 그 경계에서 나왔다 |
| 3 | **`configASSERT` 무효** | Release 빌드에서 `assert()` 가 제거된다. 태스크 우선순위 초과가 조용히 잘리고, NULL 핸들도 진입 시 검출되지 않는다 |

#### 코드 규모와 죽은 코드 비중

작업 대상은 `main/` 과 `port/app/` 이다. `port/boot/` 는 별도 바이너리이고
`libraries/` 는 외부 서브모듈(pico-sdk, mbedTLS, FreeRTOS, ioLibrary)이다.

| 영역 | 파일 | 줄 |
|---|---|---|
| `main/` — 부팅·태스크 생성 | 3 | 867 |
| `port/app/` — **앱 펌웨어 (작업 대상)** | 101 | 22,810 |
| `port/boot/` — 부트로더 (참고) | 35 | 8,234 |
| `libraries/` — 외부 (참고) | 7,997 | 4,511,305 |

작업 대상 합계는 약 **23,700줄**이며, 그중 실행되지 않는 코드는 다음과 같다.

| 죽은 영역 | 줄 | 상태 |
|---|---|---|
| 구 이더넷-직렬 중계 모듈 (파일 1개, 2,368줄) | 2,052 | 이 파일 안에서만 87%. 나머지 316줄은 커맨드 모드 이스케이프와 장치 상태 조회 경로로 **살아있다** |
| FreeMODBUS 스택 (14개 파일) | 929 | 진입점이 없어 전체가 죽어 있다. 현재 Modbus 구현은 이 스택을 쓰지 않는 별도 구현체다 |
| **소계** | **약 2,980** | 앱 펌웨어의 약 **13%** |

여기에 설치되지 않는 수신 처리기 2개, 생성되지 않는 동기화 객체 9개가 더 있다.

---

## 11. 권장 착수 순서

1. **값 신선도** (10.3-1) — 감시 장비의 기본 요건이며 다른 작업과 독립적이다
2. **미구현 프로토콜 표시** (10.2-2) — 작고, 오해를 막는 효과가 크다
3. **UPS 전용 프로토콜 구현** (10.2-1)
4. **디버그 포트** (10.4-5) — 3번의 개발·검증을 크게 돕는다
5. 원 요구사항 3종 (10.4-4)
6. **죽은 코드 일괄 정리** (10.5-2) — 위가 모두 끝난 뒤 한 번에
